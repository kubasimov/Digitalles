<Window x:Class="WPF.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.View"
        mc:Ignorable="d"
        Title="MainView" Height="300" Width="300" DataContext="{Binding Main, Mode=OneWay, Source={StaticResource Locator}}">
    <Grid>
        <Button Content="Ocr" HorizontalAlignment="Left" Margin="125,76,0,0" VerticalAlignment="Top" Width="75" Command="{Binding OcrCommand, Mode=OneWay}"/>
        <Button Content="Exit" HorizontalAlignment="Left" Margin="125,101,0,0" VerticalAlignment="Top" Width="75" Command="{Binding ExitCommand, Mode=OneWay}"/>

    </Grid>
</Window>
